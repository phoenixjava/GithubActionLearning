name: Environment Test

on:
  workflow_dispatch:

jobs:
  environment_test:
    runs-on: ubuntu-latest
    environment: phoenix_prd
    
    steps:       
      - name: environment test
        env:
          PRD_AKS: ${{ secrets.PRD_AKS }}
        #if: ${{ github.event.inputs.test_password == 'qa' }} 
        run: |
         echo "phoenix test " $PRD_AKS
