name: Deployment


on:
  workflow_dispatch

#on:
#  push:
#    branches:
#      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: phoenix_prd
    steps:
      - name: deploy
        # ...deployment-specific steps
        run: echo "just a test"
